\section{K近邻模型}

下图展示了KNN分类的基本思想：以待预测点为中心，逐渐扩大邻域，直到包含$k$个训练样本，再由这些邻居的类别决定预测结果。图中取$k=5$，邻域内有$3$个A类样本和$2$个B类样本，因此查询点被预测为A类。
\begin{figure}[H]
	\centering
	\begin{tikzpicture}[
		x=1.25cm,
		y=1.05cm,
		classA/.style={circle, draw=blue!70!black, fill=blue!65, minimum size=6.5pt, inner sep=0pt},
		classB/.style={regular polygon, regular polygon sides=3, draw=orange!85!black, fill=orange!80, minimum size=8pt, inner sep=0pt},
		query/.style={star, star points=5, star point ratio=2.1, draw=black, fill=yellow!80!orange, minimum size=11pt, inner sep=0pt},
	]
		\draw[step=1, gray!13, very thin] (0,0) grid (6,5.5);
		\draw[->, very thick, black!85] (0,0) -- (6.25,0) node[right, text=black] {特征$x_1$};
		\draw[->, very thick, black!85] (0,0) -- (0,5.75) node[above, text=black] {特征$x_2$};

		\coordinate (querypoint) at (3.2,2.8);
		\draw[very thick, dashed, teal!75!black, fill=teal!8]
			(querypoint) circle[radius=1.55];
		\node[query] at (querypoint) {};
		\node[anchor=south west, font=\small\bfseries] at (3.28,2.9) {查询点};
		\node[font=\small, text=teal!70!black, fill=white, inner sep=1.5pt]
			at (3.2,4.43) {$k=5$邻域};

		% 邻域内的五个样本：A类三个，B类两个
		\node[classA] at (2.4,2.5) {};
		\node[classA] at (3.6,2.0) {};
		\node[classA] at (3.8,3.5) {};
		\node[classB] at (2.7,3.7) {};
		\node[classB] at (4.5,2.7) {};

		% 邻域外样本
		\node[classA] at (0.8,0.8) {};
		\node[classA] at (1.3,4.8) {};
		\node[classA] at (5.3,4.5) {};
		\node[classB] at (0.8,3.0) {};
		\node[classB] at (2.8,0.7) {};
		\node[classB] at (5.4,1.1) {};
		\node[classB] at (4.9,5.0) {};

		\node[classA] (legendA) at (0.75,5.35) {};
		\node[anchor=west, font=\small] at (0.9,5.35) {A类};
		\node[classB] (legendB) at (2.0,5.35) {};
		\node[anchor=west, font=\small] at (2.17,5.35) {B类};
		\node[query] at (3.35,5.35) {};
		\node[anchor=west, font=\small] at (3.53,5.35) {待预测};
		\node[
			draw=teal!60!black,
			fill=white,
			rounded corners=2pt,
			align=center,
			font=\small,
			inner sep=4pt
		] at (5.2,2.05) {邻居投票\\A类：$3$\\B类：$2$};
	\end{tikzpicture}
	\caption{KNN分类示意图。虚线圆仅用于表示包含第$5$个最近邻的最小邻域；实际算法只需比较距离，不必显式构造圆形边界。}
	\label{fig:knn-neighborhood}
\end{figure}

\begin{definition}
	设$\mathcal{D}=\{(x_i,y_i)\}_{i=1}^{n}$为观测样本集，其中$x_i$为第$i$个样本的特征向量，$y_i$是第$i$个样本的标签或值。给定距离度量$\rho$以及整数$k\geqslant1$，对任意样本$x$，记其$k$个最近邻样本索引集合为：
	\begin{equation*}
		\mathcal{N}_k(x)=\underset{S\subset\{1,\dots,n\},|S|=k}{\arg\min}\sum_{i\in S}\rho(x,x_i)
	\end{equation*}
	则称如下规则为\gls{KNN}模型：
	\begin{enumerate}
		\item \textbf{分类情形：}
		\begin{equation*}
			\hat y(x)=\underset{c\in\mathcal{Y}}{\arg\max}\sum_{i\in\mathcal{N}_k(x)}\mathbf{1}(y_i=c)
		\end{equation*}
		\item \textbf{回归情形：}
		\begin{equation*}
			\hat y(x)=\frac{1}{k}\sum_{i\in\mathcal{N}_k(x)}y_i
		\end{equation*}
	\end{enumerate}
	若进一步引入权重函数$w_i(x)$，则可得到加权KNN估计：
	\begin{equation*}
		\hat y(x)=\sum_{i\in\mathcal{N}_k(x)}w_i(x)y_i,\qquad\sum_{i\in\mathcal{N}_k(x)}w_i(x)=1
	\end{equation*}
	其中权重通常取为距离的单调递减函数。
\end{definition}
KNN属于一种基于实例(instance-based)的非参数学习方法，其核心思想是在局部邻域中进行统计决策。模型本身不存在显式训练阶段，计算复杂度主要集中在查询阶段的最近邻搜索，因此在高维数据中通常结合KD-Tree或Ball-Tree等空间索引结构以降低搜索成本。
