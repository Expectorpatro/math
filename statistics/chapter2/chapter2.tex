\chapter{something}

\begin{definition}
	设$\mathscr{P}$为可测空间$(X,\mathscr{A})$上的一个概率测度族，则称$(X,\mathscr{A},\mathscr{P})$为\textbf{统计结构}或\textbf{统计模型}。若$\mathscr{P}$仅依赖于参数$\theta$，即：
	\begin{equation*}
		\mathscr{P}=\{P_{\theta}:\theta\in\Theta\}
	\end{equation*}
	其中$\Theta$为参数空间，则称此结构为\textbf{参数结构}，否则称为\textbf{非参数结构}。
\end{definition}
\begin{definition}
	设$(X_1,\mathscr{A}_1,\mathscr{P}_1),(X_2,\mathscr{A}_2,\mathscr{P}_2),\dots,(X_n,\mathscr{A}_n,\mathscr{P}_n)$为$n$个统计结构，称：
	\begin{equation*}
		\left(\otimes_{i=1}^nX_i,\otimes_{i=1}^n\mathscr{A}_i,\otimes_{i=1}^n\mathscr{P}_i\right)
	\end{equation*}
	为它们的\textbf{乘积结构}，其中$\otimes$表示Cartisian积。
\end{definition}
\begin{definition}
	设$(X,\mathscr{A},\mathscr{P})$为统计结构。若可测空间$(X,\mathscr{A})$上存在一个$\sigma$有限测度$\mu$满足对任意的$P\in\mathscr{P}$有$P\ll\mu$，则称$(X,\mathscr{A},\mathscr{P})$是\textbf{可控的}。
\end{definition}
\begin{definition}
	设$(X,\mathscr{A},\mathscr{P})$为统计结构，$T$是可测空间$(X,\mathscr{A})$到可测空间$(Y,\mathscr{B})$的可测映射。若$T$不依赖于$\mathscr{P}$，则称$T$为$(X,\mathscr{A},\mathscr{P})$上的\textbf{统计量}。统计量的分布称为\textbf{抽样分布}或\textbf{诱导分布}。
\end{definition}
在测度论的可测映射那一小节我们提到了概率分布，接下来我们所提到的统计量的分布若无特殊说明都默认为是概率分布。
\begin{property}
	设$f$是可测空间$(X,\mathscr{A})$到可测空间$(Y,\mathscr{B})$上的统计量，$(X,\mathscr{A},\mathscr{P})$是统计结构。则：
	\begin{enumerate}
		\item 对于任意的$P\in\mathscr{P}$，概率分布$Pf^{-1}$是$(Y,\mathscr{B})$上的概率测度；
		\item 若$(X,\mathscr{A},\mathscr{P})$是可控的，则$(Y,\mathscr{B},\mathscr{P}f^{-1})$也可控，其中$\mathscr{P}f^{-1}=\{Pf^{-1}:P\in\mathscr{P}\}$；
		\item 若$(X,\mathscr{A},\mathscr{P})$有概率函数族$\left\{\dfrac{\dif P}{\dif\mu}\right\}$，则$(Y,\mathscr{B},\mathscr{P}f^{-1})$有概率函数族$\left\{\dfrac{\dif Pf^{-1}}{\dif\mu f^{-1}}\right\}$。
	\end{enumerate}
\end{property}
\begin{proof}
	(1)由\cref{prop:MeasurableMapping}(3)即可得到。\par
	(2)因为$(X,\mathscr{A},\mathscr{P})$是可控的，所以存在$(X,\mathscr{A})$上的一个$\sigma$有限测度$\mu$满足对任意的$P\in\mathscr{P}$有$P\ll\mu$，由\cref{prop:MeasurableMapping}(3)可知$\mu f^{-1}$也是$\sigma$有限测度。若集合$A\in\mathscr{B}$满足$\mu f^{-1}(A)=0$，即$\mu[f^{-1}(A)]=0$，因为$P\ll\mu$，所以$Pf^{-1}(A)=P[f^{-1}(A)]=0$，于是$Pf^{-1}\ll\mu f^{-1}$。根据$P$的任意性，$(Y,\mathscr{B},\mathscr{P}f^{-1})$可控。\par
	(3)由(2)即可得到。
\end{proof}

\input{statistics/chapter2/order-statistics.tex}
\input{statistics/chapter2/sufficient-statistics.tex}
\input{statistics/chapter2/deltamethod.tex}
